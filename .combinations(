# Enter your code here. Read input from STDIN. Print output to STDOUT
from itertools import combinations
S, k = input().split()
k = int(k)
S_sorted = sorted(S)
for i in range(1, k + 1):
    combos = list(combinations(S_sorted, i))
    for combo in combos:
        print("".join(combo))
